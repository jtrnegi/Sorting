#include<stdio.h>
#include<conio.h>
void main()
{
int arr[6]={12.32,35,34,21,46};
int hole,value;
for(i=1;i<n;i++)
{
hole=i;
value=arr[i];
{
while(hole>0 && arr[hole-1]>value)
{
arr[hole]=arr[hole-1];
hole=hole-1;
}
a[hole]=value;
}
 for(i=0;i<n;i++)
 {
 printf("%d\n",arr[i]);
 }
 getch();
 }
